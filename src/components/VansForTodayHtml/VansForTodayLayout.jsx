import React from 'react'
import { Outlet } from "react-router-dom";

/**
 * Layout should include two links: 
 * 1) - Van Morph results (editable via drag and drop)
 *    - Includes absent van, updated vans, Vans not updated but have space for new passengers
 * 2) Today's final Van organiation (not editable)
 */

export default function VansForTodayLayout() {
  return (
    <div>VansForTodayLayout</div>
  )
}
